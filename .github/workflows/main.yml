name: BADIA_CICD
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# env:
#   DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
#   DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
#   MDP_GOOGLE: ${{ secrets.MDP_GOOGLE }}
#   POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}

jobs:
  badia_pylint:
    runs-on: ubuntu-latest
    steps:
      - name: Log runner resources
        run: |
          echo "=== Disque ==="; df -h
          echo "=== RAM ==="; free -h
          echo "=== Docker ==="; docker system df

      - name: Récupération du code
        uses: actions/checkout@v4

      - name: Installer Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12.12'

      - name: Installer dépendances
        run: |
          uv sync

      - name: Log runner resources
        run: |
          echo "=== Disque ==="; df -h
          echo "=== RAM ==="; free -h
          echo "=== Docker ==="; docker system df

    #   - name: Lancer pylint
    #     run: pylint app/app.py --fail-under=7

    #   - name: Nettoyer l’espace disque
    #     if: always()
    #     run: |
    #       rm -rf ~/.cache/pip
    #       df -h

    #   - name: Log runner resources
    #     run: |
    #       echo "=== Disque ==="; df -h
    #       echo "=== RAM ==="; free -h
    #       echo "=== Docker ==="; docker system df
    # runs-on: ubuntu-latest
    # steps:
    #   - name: Repo checkout
#         uses: actions/checkout@v3
#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: "3.12.12"
#       - name: Cache pip packages
#         uses: actions/cache@v4
#         with:
#           path: ~/.cache/pip
#           key: ${{ runner.os }}-pip-${{ hashFiles('requirements_raw_to_jpeg.txt') }}
#           restore-keys: |
#             ${{ runner.os }}-pip-

#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements_raw_to_jpeg.txt
#       - name: Where am I
#         run: |
#           pwd
#           ls -la
#           pip list
#       - name: Run raw_to_jpeg.py
#         env:
#           MDP_MAIL: ${{ secrets.MDP_MAIL }}
#           MAIL_SENDER_ADRESS: ${{ secrets.MAIL_SENDER_ADRESS }}
#           MAIL_RECIEVER: ${{ secrets.MAIL_RECIEVER }}
#           SMTP_HOST: ${{ secrets.SMTP_HOST }}
#           SMTP_PORT: ${{ secrets.SMTP_PORT }}
#           ROOT_FOLDER_NAME: ${{ secrets.ROOT_FOLDER_NAME }}
#           ROOT_FOLDER_ID: ${{ secrets.ROOT_FOLDER_ID }}
#           VIDEO_RAW_FOLDER_NAME: ${{ secrets.VIDEO_RAW_FOLDER_NAME }}
#           VIDEO_RAW_FOLDER_ID: ${{ secrets.VIDEO_RAW_FOLDER_ID }}
#           PHOTO_RAW_FOLDER_NAME: ${{ secrets.PHOTO_RAW_FOLDER_NAME }}
#           PHOTO_RAW_FOLDER_ID: ${{ secrets.PHOTO_RAW_FOLDER_ID }}
#           CLEANED_DATA_FOLDER_NAME: ${{ secrets.CLEANED_DATA_FOLDER_NAME }}
#           CLEANED_DATA_FOLDER_ID: ${{ secrets.CLEANED_DATA_FOLDER_ID }}
#           TREATED_LIST_FOLDER: ${{ secrets.TREATED_LIST_FOLDER }}
#           TREATED_LIST_FOLDER_ID: ${{ secrets.TREATED_LIST_FOLDER }}
#           GDRIVE_SERVICE_ACCOUNT_KEY: ${{ secrets.GDRIVE_SERVICE_ACCOUNT_KEY }}
#           SCOPES: ${{ secrets.SCOPES }}
          
#         run: |
#           python raw_to_jpeg.py
#       - name: Persist log file
#         uses: actions/upload-artifact@v4
#         with:
#           name: last_log_file
#           path: ./logs/raw_to_jpeg.log

# name: My Deploy
# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# env:
#   DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
#   DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
#   MDP_GOOGLE: ${{ secrets.MDP_GOOGLE }}
#   POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
    
# jobs:


#   unit-tests:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Log runner resources
#         run: |
#           echo "=== Disque ==="; df -h
#           echo "=== RAM ==="; free -h
#           echo "=== Docker ==="; docker system df

#       - name: Récupération du code
#         uses: actions/checkout@v4

#       - name: Installer Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.12'

#       - name: Log runner resources
#         run: |
#           echo "=== Disque ==="; df -h
#           echo "=== RAM ==="; free -h
#           echo "=== Docker ==="; docker system df

#       - name: Installer dépendances
#         run: |
#           python -m pip install --upgrade pip
#           pip install --no-cache-dir -r app/requirements.txt

#       - name: Log runner resources
#         run: |
#           echo "=== Disque ==="; df -h
#           echo "=== RAM ==="; free -h
#           echo "=== Docker ==="; docker system df
          

#       - name: Exécuter les tests unitaires
#         run: pytest -s -vv -W ignore::DeprecationWarning tests_unitaires

#       - name: Nettoyer l’espace disque
#         if: always()
#         run: |
#           rm -rf ~/.cache/pip
#           df -h
      
#       - name: Log runner resources
#         run: |
#           echo "=== Disque ==="; df -h
#           echo "=== RAM ==="; free -h
#           echo "=== Docker ==="; docker system df

#   build-and-integration-tests:
#     needs: [unit-tests, pylint]
#     runs-on: ubuntu-latest
#     env:
#       POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
#     steps:

#       - name: Log runner resources
#         run: |
#           echo "=== Disque ==="; df -h
#           echo "=== RAM ==="; free -h
#           echo "=== Docker ==="; docker system df

#       - name: Nettoyage disque avant Docker Compose
#         run: |
#           echo "=== Nettoyage pip cache ==="
#           rm -rf ~/.cache/pip
#           echo "=== Vérification disque ==="
#           df -h
#           docker system df

#       - name: Log runner resources
#         run: |
#           echo "=== Disque ==="; df -h
#           echo "=== RAM ==="; free -h
#           echo "=== Docker ==="; docker system df

#       - name: Récupérer le code
#         uses: actions/checkout@v4

#       - name: Log in to DockerHub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}

#       - name: Démarrer les conteneurs avec docker-compose
#         run: docker compose up -d
      
#       - name: Log runner resources
#         run: |
#           echo "=== Disque ==="; df -h
#           echo "=== RAM ==="; free -h
#           echo "=== Docker ==="; docker system df

#       - name: Vérifier les conteneurs
#         run: docker ps -a

#       - name: Attendre que Flask soit prêt
#         run: |
#           echo "Attente que Flask écoute sur le port 5000..."
#           timeout 60 bash -c 'until curl -s http://localhost:5000/health; do sleep 2; done'

#       - name: Installer Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.12'
      
#       - name: Log runner resources
#         run: |
#           echo "=== Disque ==="; df -h
#           echo "=== RAM ==="; free -h
#           echo "=== Docker ==="; docker system df

#       - name: Installer dépendances
#         run: |
#           python -m pip install --upgrade pip
#           pip install --no-cache-dir -r app/requirements_post_build.txt

#       - name: Exécuter les tests d'intégration
#         run: pytest -s -vv -W ignore::DeprecationWarning integration_tests

#       - name: Nettoyer Docker et pip
#         if: always()
#         run: |
#           docker compose down -v --rmi all
#           docker system prune -af
#           rm -rf ~/.cache/pip
#           df -h

#   deploy-on-Render:
#     needs: build-and-integration-tests
#     runs-on: ubuntu-latest
#     steps:
#       - name: déploiement sur render
#         run: echo "Ici le dépoiement sur Render... Si la version gratuite permet le déploiement de plusieurs conteneurs..."